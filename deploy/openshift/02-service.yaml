# 02 — The Service.
apiVersion: v1
kind: Service
metadata:
  name: mom-prompt-service
  namespace: mom-ai
  labels:
    app: mom-prompt-service
spec:
  selector:
    app: mom-prompt-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
